{
  "version": 2,
  "buildCommand": "cd frontend && yarn install --frozen-lockfile && yarn build",
  "outputDirectory": "frontend/build",
  "installCommand": "echo skip-root-install",
  "framework": null,
  "functions": {
    "api/index.py": {
      "runtime": "@vercel/python@4.3.0",
      "maxDuration": 60,
      "excludeFiles": "{frontend/**,backend/**,tests/**,test_reports/**,memory/**,scripts/**,plugins/**,node_modules/**,**/node_modules/**,**/__pycache__/**,**/*.pyc,.git/**,.emergent/**,.vercel/**,.github/**,*.md,blackbox-2.jsx,test_result.md,VERCEL_DEPLOY.md,README.md}"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/api", "destination": "/api/index" }
  ],
  "headers": [
    {
      "source": "/static/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    }
  ]
}
